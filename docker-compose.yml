version: '3'

services:
  mongodb:
    container_name: sales-db
    image: mongo:8.0.0-rc4
    restart: always
    environment:
      MONGO_INITDB_DATABASE: sales-db
    ports:
      - "27017:27017"
    volumes:
      - sales_db:/data/db

volumes:
  sales_db:
    driver: local
